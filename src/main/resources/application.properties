spring.application.name=com.prefeitura

# Conexão com o banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/site_transparencia
spring.datasource.username=postgres
spring.datasource.password=pwdokiet

# Mágica do Hibernate: Cria as tabelas automaticamente no banco!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
